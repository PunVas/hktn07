apiVersion: v1
kind: Service
metadata:
  name: pr-guardian
  namespace: default
  labels:
    app: pr-guardian
spec:
  type: ClusterIP
  selector:
    app: pr-guardian
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  sessionAffinity: None
