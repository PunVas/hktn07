apiVersion: v1
kind: Service
metadata:
  name: pr-guardian
  namespace: hank-ns-7
  labels:
    app: pr-guardian
spec:
  type: LoadBalancer
  selector:
    app: pr-guardian
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  sessionAffinity: None
